# Development

- When implementing features and code changes, ALWAYS use the tdd-unit-test skill for rigorous TDD
- This is a nextjs app; when we need to modify Next setup we should apply the nextjs-application skill and match its patterns
- We should use our `package.json` npm commands to execute common dev functionality
